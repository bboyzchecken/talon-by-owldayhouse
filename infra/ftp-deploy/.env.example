# Copy to infra/ftp-deploy/.env and fill in. NEVER commit the real .env.
FTP_HOST=ftp.yourhost.com
FTP_USER=your-ftp-user
FTP_PASS=your-ftp-password
FTP_REMOTE_DIR=/public_html

# optional
# FTP_PORT=21
# FTP_PROTO=ftps   # use FTP over TLS if the host supports it
